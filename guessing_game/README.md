# Things we learnt

- Manage dependencies via Cargo.toml
- Cargo.lock is an artefact that is deployed and pins all your dependencies
- Run VSCode via the terminal to get debugging
- `let` vs `let mut` the latter enables the var to be mutable
- `.expect` vs. using `match` you can have a logic within a varible defintion!
- `loop` does what it says on the tin
- `thread_rng`, lots to think about
- `rustfmt src/main.rs`